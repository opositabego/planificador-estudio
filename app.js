// Código base para Planificador de Estudio con drag & drop, exportación y organización
// Por simplicidad, esto es el esqueleto. El contenido detallado se añadirá luego.

const App = () => {
  return (
    <div className="container">
      <h1>Planificador de Estudio</h1>
      <p>Este esqueleto incluye el sistema de tareas con drag & drop y exportación a Excel.</p>
    </div>
  );
};

ReactDOM.render(<App />, document.getElementById("root"));
